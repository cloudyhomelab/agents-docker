name: build and push image

on:
  workflow_dispatch:
    inputs:
      agents:
        description: "Agents to build (comma-separated)"
        type: string
        default: "claude,codex,gemini"
  push:
    branches:
      - main
    paths-ignore:
      - ".github/**"
      - "README.md"

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}

env:
  REGISTRY: docker.io

jobs:
  build:
    runs-on: ubuntu-latest

    permissions:
      contents: read
      id-token: write

    steps:
      - name: checkout repository
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1

      - name: detect changed agents
        id: detect
        run: |
          changed=$(git diff HEAD~1 HEAD -- docker-bake.hcl \
            | grep '^+variable' \
            | grep -oP '(?<=")(CLAUDE|CODEX|GEMINI)(?=_VERSION")' \
            | tr '[:upper:]' '[:lower:]' \
            | tr '\n' ',' | sed 's/,$//')
          echo "agents=${changed:-claude,codex,gemini}" >> "$GITHUB_OUTPUT"

      - name: login to docker hub
        uses: docker/login-action@dbcb813823bdd20940b903addbd779551569679f # v4.6.0
        with:
          registry: ${{ env.REGISTRY }}
          username: ${{ secrets.DOCKERHUB_USERNAME }}
          password: ${{ secrets.DOCKERHUB_TOKEN }}

      - name: set up docker buildx
        uses: docker/setup-buildx-action@37fe631027851001ddb9b187196cc803df7f5f0e # v4.3.0
        with:
          driver: docker-container

      - name: build and push
        id: bake
        uses: docker/bake-action@d3418bd7d0e9324001bca92fa8ba175ea7e6dc9b # v7.3.0
        env:
          BUILD_AGENTS: ${{ inputs.agents || steps.detect.outputs.agents }}
        with:
          targets: default
          push: true
          set: |
            *.cache-from=type=gha
            *.cache-to=type=gha,mode=max
            *.attest=type=provenance,mode=max
            *.attest=type=sbom,generator=docker/scout-sbom-indexer:latest

      - name: install cosign
        uses: sigstore/cosign-installer@6f9f17788090df1f26f669e9d70d6ae9567deba6 # v4.1.2

      - name: sign published image(s) (keyless)
        run: |
          set -euo pipefail

          cat > bake-metadata.json <<'JSON'
          ${{ steps.bake.outputs.metadata }}
          JSON

          # Extract tag@digest refs from Bake metadata and sign them.
          jq -r '
            to_entries[]
            | .value as $v
            | ($v["containerimage.digest"] // empty) as $digest
            | ($v["image.name"] // empty) as $names
            | if ($digest == "" or $names == "") then empty else
                (if ($names|type) == "string" then ($names|split(",")) else $names end)
                | map(gsub("^\\s+|\\s+$"; ""))
                | map(select(length>0))
                | .[]
                | "\(.)@\($digest)"
              end
          ' bake-metadata.json | sort -u | while read -r ref; do
            echo "Signing ${ref}"
            cosign sign --yes "${ref}"
          done
